name: video_compress_example
description: Demonstrates how to use the video_compress plugin.
publish_to: 'none'

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  #图片选择器
  image_picker: ^0.6.1
#  path_provider: any
  video_compress:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

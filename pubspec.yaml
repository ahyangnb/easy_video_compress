name: easy_video_compress
description: A new flutter plugin project.
version: 0.0.1
#author:
homepage: https://github.com/ahyangnb/easy_video_compress

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  plugin:
    platforms:
      android:
        package: com.easy.compress.easy_video_compress
        pluginClass: EasyVideoCompressPlugin
      ios:
        pluginClass: EasyVideoCompressPlugin
